# TODO

難易度目安: 小（数時間）/ 中（1〜2日）/ 大（数日以上）

## 未着手

- [ ] **局面のURL共有** `難易度: 小`
  - 確率計算モードの盤面をクエリパラメータに載せ、リンクで共有・復元できるようにする
  - 影響: `useTurnState` に URL との同期を追加

- [ ] **継続時の期待値を画面に出す** `難易度: 小`
  - 「つよい」AIが内部で計算している「もう1回振ったときの期待進捗」を、対戦モードのプレイヤー側にも表示する
  - `ai.ts` の `expectedValueAfterRoll()` をそのまま使える
  - 影響: 表示だけ。ただし「答えを見ながら」になるのでトグルの扱いを検討する

- [ ] **AIをもう1段強くする** `難易度: 中`
  - 現在の「つよい」は1手先読みで、相手の進捗を見ていない
  - 相手が王手（あと1列）のときはリスクを取る、といった局面判断を入れる
  - 影響: `ai.ts` の評価関数に相手の状態を渡す。強さの検証は `scripts/bench.ts` で測れる

- [ ] **3〜4人対戦** `難易度: 中`
  - 現在は人間1 vs AI1 の固定
  - 影響: `game.ts` の `PlayerId` と `progress` を配列に一般化、盤面の色分けを人数分に拡張

## 完了

- [x] **AI対戦モード** — 3段階の強さのAIと1対1で対局できる
- [x] **戦略のシミュレーション比較** — `scripts/bench.ts` で戦略を総当たり対戦させて勝率表を出す
- [x] **GitHub Pages の有効化** — `main` への push でデプロイが自動実行される
